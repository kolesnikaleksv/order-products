import AddButton from '../components/AddButton/AddButton';

export default {
  title: 'AddButton',
  component: AddButton,
  tags: ['autodocs'],
};

export const Primary = {
  args: {
    primary: true,
    size: 'normal'
  },
};

export const small = {
  args: {
    size: 'small'
  }
}